server:
  port: 8502
  servlet:
    context-path: /recurso

eureka:
  instance:
    hostname: localhost
    port: 8505
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000